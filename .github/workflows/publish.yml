name: Publish

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: publish-main
  cancel-in-progress: false

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: write
      # necessario para o npm trocar o token OIDC do Actions (trusted publishing)
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org

      # trusted publishing exige npm >= 11.5.1
      - name: Atualiza npm
        run: npm install -g npm@latest

      - run: npm ci

      - name: Resolve versao
        id: pkg
        run: |
          NAME=$(node -p "require('./package.json').name")
          VERSION=$(node -p "require('./package.json').version")
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if [ -n "$(npm view "$NAME@$VERSION" version --silent 2>/dev/null || true)" ]; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "::notice::$NAME@$VERSION ja publicado — pulando publish."
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        if: steps.pkg.outputs.published == 'false'
        run: npm run build

      # sem NODE_AUTH_TOKEN: a auth vem do OIDC do proprio Actions.
      # provenance eh gerado automaticamente pelo trusted publishing.
      - name: Publish
        if: steps.pkg.outputs.published == 'false'
        run: npm publish --access public --loglevel verbose

      - name: Tag da release
        if: steps.pkg.outputs.published == 'false'
        run: |
          TAG="v${{ steps.pkg.outputs.version }}"
          git tag "$TAG"
          git push origin "$TAG"
